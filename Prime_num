for num in range(2, 101):
    prime = True
        print(num)
	    for p in range(2,num):
	            if (num%p==0):
		                prime = False
				    if prime:
				            print(" ^""-is a prime number")
